/**
 * AVANT — API Configuration
 * All keys below are FREE — no credit card needed
 * Fill in your keys here
 */

// ── YOUR KEYS (you already have this) ──────────────────────
export const OPENAI_API_KEY = 'YOUR_OPENAI_API_KEY';

// ── FREE — console.groq.com (no credit card) ───────────────
export const GROQ_API_KEY = 'YOUR_GROQ_API_KEY';

// ── FREE — aistudio.google.com/apikey ──────────────────────
export const GEMINI_API_KEY = 'YOUR_GEMINI_API_KEY';

// ── FREE 100/month — serpapi.com ───────────────────────────
export const SERPAPI_KEY = 'YOUR_SERPAPI_KEY';

// ── FREE — openweathermap.org/api ──────────────────────────
export const OPENWEATHER_KEY = 'YOUR_OPENWEATHER_KEY';

// ── NASA (free, demo key works for testing) ─────────────────
export const NASA_API_KEY = 'DEMO_KEY'; // Replace at api.nasa.gov

// ── App Settings ───────────────────────────────────────────
export const OWNER_NAME    = 'Michael';
export const WAKE_WORD     = 'avant';
export const HOME_ADDRESS  = 'Your Home Address Here';
export const TIMEZONE      = 'America/Los_Angeles';

// ── Voice Settings ─────────────────────────────────────────
export const VOICE_PITCH   = 1.1;   // Slightly higher = more feminine
export const VOICE_RATE    = 0.95;  // Slightly slower = more natural
export const VOICE_LANG    = 'en-US';
